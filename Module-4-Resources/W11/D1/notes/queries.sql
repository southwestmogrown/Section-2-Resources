-- EXPLAIN QUERY PLAN
SELECT * FROM students WHERE first_name = 'Briana';

-- CREATE INDEX idx_students_first_name_last_name
-- ON students (first_name, last_name);


-- EXPLAIN QUERY PLAN
SELECT * FROM students WHERE first_name = 'Briana';